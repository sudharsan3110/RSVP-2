type PropsWithClassName = {
  className?: string;
};

type FormSelectOption = {
  value: string;
  label: string;
};

type EmailAttachmentOptions = {
  filename: string;
  path: string;
  cid: string;
};
